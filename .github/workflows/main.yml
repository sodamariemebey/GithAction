name: Hello World

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  dire-bonjour:
    runs-on: ubuntu-latest
    steps:
      - name: Dire bonjour
        run: echo "Bonjour GitHub Actions 👋"
